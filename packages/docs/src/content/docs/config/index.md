---
title: Configuration Reference
description: Config file formats, schema, template variables, and state backends.
---

Reference material for the config file itself — separate from the CLI that reads it.

- **File formats** — `.ts`, `.js`, `.cjs`, `.json`, `.yaml`, `.toml`
- **Top-level schema** — the shape of a `golde.config.*`
- **Template variables** — `env`, `git`, `file`, `config`, `resources`
- **State backends** — S3, filesystem, Golde Cloud
- **[Outputs & Hooks](/config/outputs-and-hooks/)** — named output values and lifecycle notifications (Discord, Slack, webhooks)

*Pages coming soon.*
