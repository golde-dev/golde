---
title: Concepts
description: How Golde models infrastructure, configuration, and deployments.
---

The mental model behind Golde. Read these once and the rest of the docs make sense.

- **How Golde works** — plan, apply, and the resource lifecycle
- **Configuration files** — `golde.config.ts` and the alternative formats
- **Templates and variables** — `{{ env }}`, `{{ git }}`, `{{ state }}`, and resolution order
- **Branch-based deployments** — `branch` and `branchPattern`
- **State and state backends** — S3, filesystem, and Golde Cloud
- **Dependencies and execution order** — how the dependency graph is built
- **Versioned resources** — versioned deploys, rollbacks, and `maxVersions`

*Pages coming soon.*
